{
  "recorded_at": "2026-09-08T21:15:00+00:00",
  "box": "Crash/fault tests at every durable transition and concurrent remote edits (milestone 2)",
  "method": "Enumerated the journal modules containing fsync or transaction commits, then read the tests that reach each. Test names were not used as evidence; see docs/development.md on why that undercounts here.",
  "durable_modules": 11,
  "with_a_kill_point": 11,
  "rows": [
    {
      "module": "journal.rs",
      "crash_test": "tests/upload_journal.rs: actual_process_death_preserves_pending_uncertain_and_acknowledged_states",
      "note": "Kills the process mid-queue and asserts pending, uncertain and acknowledged records each survive in their own state."
    },
    {
      "module": "journal/working.rs",
      "crash_test": "tests/working_files.rs: killed_process_preserves_fsynced_save_and_newer_unsealed_changes",
      "note": "SIGKILL between a sealed save and later unsealed writes; the fsynced payload survives verbatim and the newer bytes stay readable."
    },
    {
      "module": "journal/mutations.rs",
      "crash_test": "tests/mutations.rs: actual_sigkill_preserves_pending_outcome_and_acknowledged_receipt",
      "note": "SIGKILL around a mutation; a pending outcome and an acknowledged receipt both survive."
    },
    {
      "module": "journal/namespace.rs",
      "crash_test": "tests/namespace_objects.rs + namespace_crash_child",
      "note": "Subprocess killed during namespace publication."
    },
    {
      "module": "journal/preparation.rs",
      "crash_test": "tests/upload_preparation.rs: actual_process_death_keeps_a_claimed_preparation_unfinished_and_a_completed_one_whole",
      "note": "SIGKILL at both durable transitions. Killed after claim_preparation, the record stays Preparing with no adoptable payload -- a placeholder there would upload an empty replacement over a real document. Killed after complete_preparation, the captured source survives verbatim. Verified discriminating: removing the child's ready signal makes it time out and fail."
    },
    {
      "module": "journal/generations.rs",
      "crash_test": "Transitively: tests/upload_journal.rs journal_crash_fixture calls claim_next, which runs resolve_ready_generations -- the only runtime commit in this module.",
      "note": "Its other two commits are in migrate and migrate_dependencies, covered by version_five_migration_keeps_existing_save_lineage_and_refuses_newer_schema. No separate kill point: the runtime transition has no entry point of its own."
    },
    {
      "module": "journal/handoff.rs",
      "crash_test": "tests/clean_handoff.rs: actual_process_death_keeps_detached_bytes_on_disk",
      "note": "SIGKILL after the handoff and after cleanup. The handoff is durable across the kill and the detached bytes are still on disk when the process dies. What the NEXT journal open does to them is an open question, below."
    },
    {
      "module": "journal/replacements.rs",
      "crash_test": "tests/namespace_replacement.rs: actual_process_death_preserves_the_swapped_name_and_the_reader_gate",
      "note": "SIGKILL after the swap and after releasing the reader gate. The swapped name survives -- a crash must not restore the replaced document's name -- the victim stays unlinked, and its bytes remain readable for a descriptor that was already holding them. Verified discriminating."
    },
    {
      "module": "journal/unlinked.rs",
      "crash_test": "tests/unlinked_files.rs: actual_process_death_keeps_a_file_unlinked_and_still_owes_its_removal",
      "note": "SIGKILL after the unlink and after the removal mutation is claimed. The name stays hidden even though the remote still reports the file -- a crash must not resurrect a deleted one -- and the removal owed to the cloud is never simply gone. Killing after the claim is the harder case: the mutation is in flight, so recovery cannot assume it never started."
    },
    {
      "module": "journal/directories.rs",
      "crash_test": "tests/local_directories.rs: actual_process_death_keeps_a_pending_folder_and_its_waiting_child_consistent",
      "note": "SIGKILL before and after the folder's cloud confirmation. Before it the waiting child must stay unclaimable rather than upload to a guessed parent; after it the child must be claimable under the folder's real cloud id and not wait forever for a folder that already exists."
    },
    {
      "module": "journal/publication.rs",
      "crash_test": "Not applicable: no runtime durable transition.",
      "note": "Its single commit is inside migrate, raising user_version to 12. Schema migrations are covered by the migration tests in causal_edits.rs and clean_handoff.rs. Counting this module as an uncovered durable transition was an error in the first version of this enumeration, which grepped for commit() without asking which function it was in."
    }
  ],
  "conclusion": "Ten modules carry runtime durable transitions and all ten are now crossed by a test that kills the process: nine directly, and generations.rs transitively through claim_next, which is its only runtime commit path and has no entry point of its own. publication.rs carries only a migration.\\n\\nThis is module-level coverage, not transition-level. The milestone says every durable transition, and a module may hold several with only one crossed. The box stays unticked on that distinction, which is now the only thing between it and being ticked.",
  "cheapest_next_step": "Enumerate transitions rather than modules: one row per tx.commit() and per fsync on a runtime path, each with the kill point that crosses it. That is a mechanical pass over about twenty sites and would settle the box either way.",
  "not_reachable_autonomously": "Physical power loss and a lying fsync are a different class again, need dm-flakey and root, and the milestone text already names physical-fault coverage as an open gap.",
  "open_question_found_while_writing_this": {
    "observation": "Detached bytes survive the SIGKILL and are gone after the next UploadJournal::open. Measured by checking the spool path before and after reopening: present, then absent.",
    "why_it_matters": "docs/adr/0002-durable-local-edits.md states that detached bytes remain recovery data and that their cleanup and presentation policy is unfinished. A recovery that removes them on the next open is that policy defaulting to deletion, silently. Between a handoff and an explicit cleanup those bytes are the only local copy of an edit whose remote metadata has already moved on.",
    "not_yet_established": "Which recovery step removes them. recover_working iterates working_files, which is empty after a handoff; recover_preparation_files only matches .cirrove-preparing- names. The removal was not located, and it may be correct behaviour rather than a defect.",
    "why_the_test_does_not_pin_it": "Asserting behaviour whose intent is undecided makes a change detector rather than a guard. The test asserts what is settled -- the handoff is durable and the bytes survive the kill -- and this records the rest."
  },
  "correction": "The first version of this file counted eleven modules with durable transitions by grepping for tx.commit(). Two of those commits are schema migrations, not runtime transitions: publication.rs has only that, and two of generations.rs's four are migrate. Grepping for a call without asking which function contains it is the same error as counting test names, three files earlier in this repository's history."
}
